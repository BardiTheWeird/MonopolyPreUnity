<Window x:Class="GameInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameInterface" xmlns:customcontrols="clr-namespace:GameInterface.CustomControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <customcontrols:ScrollingTextBox x:Name="OutputWindow"
                 Grid.Row="0" Grid.Column="0" Margin="10"
                 Text="{Binding Context.OutputString}" IsReadOnly="True">
        </customcontrols:ScrollingTextBox>

        <TextBox x:Name="InputWindow" Margin="10"
                 Grid.Row="0" Grid.Column="1"
                 AcceptsReturn="True"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}">
        </TextBox>

        <Button x:Name="SendInput"
                Grid.Row="1" Grid.Column="1" Margin="10"
                Command="{Binding SendInputCommand}">
            Send Input
        </Button>

        <Button x:Name="SaveOutputLog"
                Grid.Row="1" Grid.Column="0" Margin="10"
                Command="{Binding SaveOutputLog}">
            Save Output Log
        </Button>
    </Grid>
</Window>
